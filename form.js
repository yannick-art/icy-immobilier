(function(){ window.envoyerFormulaire = function(formId){ var form = document.getElementById(formId); if(!form) return; var inputs = form.querySelectorAll('input,select,textarea'); var sourceLabel = formId === 'form-estimation' ? 'Site /estimation' : formId === 'form-contact' ? 'Site /contact' : 'Autre'; var data = {source: sourceLabel, page: window.location.pathname}; inputs.forEach(function(el){ var key = el.id || el.name || el.placeholder; if(key) data[key] = el.value; }); var webhookUrl = 'https://hook.eu1.make.com/jkvmdjx6wzez9aespn8jnl1ax48t9nu4'; var params = new URLSearchParams(); Object.keys(data).forEach(function(k){ params.append(k, data[k]); }); alert('Merci ! Votre demande a bien ete envoyee. Reponse sous 24h.'); fetch(webhookUrl, { method: 'POST', mode: 'no-cors', headers: {'Content-Type': 'application/x-www-form-urlencoded'}, body: params.toString() }).catch(function(e){ console.error('Webhook error:', e); }); }; })();

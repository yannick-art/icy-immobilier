(function(){ window.envoyerFormulaire = function(formId){ var form = document.getElementById(formId); if(!form) return; var inputs = form.querySelectorAll('input,select,textarea'); var data = {source: formId, page: window.location.pathname}; inputs.forEach(function(el){ var key = el.id || el.name || el.placeholder; if(key) data[key] = el.value; }); var webhookUrl = 'https://hook.eu1.make.com/jkvmdjx6wzez9aespn8jnl1ax48t9nu4'; alert('Merci ! Votre demande a bien ete envoyee. Reponse sous 24h.'); fetch(webhookUrl, { method: 'POST', mode: 'no-cors', headers: {'Content-Type': 'text/plain'}, body: JSON.stringify(data) }).catch(function(e){ console.error('Webhook error:', e); }); }; })();
